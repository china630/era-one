# Drive hook (Summer S4-3)

AC-C5: when Drive edition licensed, webmail may attach via platform Drive API.

Lab: set `ERA_DRIVE_API_URL` on `ui/mail`; without Drive module, compose omits hook — not a Mail Server blocker.

See Office Drive bind specs for full path.
