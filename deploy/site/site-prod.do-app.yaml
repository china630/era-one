# Копируется в корень ветки site-prod при публикации (см. site-deploy.yml).
name: era-one-site-prod
region: fra
static_sites:
  - name: web
    source_dir: /
    output_dir: /
    environment_slug: html
    error_document: 404.html
    routes:
      - path: /
