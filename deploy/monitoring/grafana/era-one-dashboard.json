{
  "annotations": { "list": [] },
  "editable": true,
  "graphTooltip": 0,
  "panels": [
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": { "defaults": { "unit": "ops" } },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 0 },
      "id": 1,
      "title": "Ingest events/s",
      "type": "timeseries",
      "targets": [
        {
          "expr": "sum(rate(era_ingest_events_total[1m]))",
          "legendFormat": "ev/s"
        }
      ]
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 0 },
      "id": 2,
      "title": "Kafka consumer lag",
      "type": "timeseries",
      "targets": [
        {
          "expr": "sum(era_kafka_consumer_lag)",
          "legendFormat": "lag"
        }
      ]
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "gridPos": { "h": 8, "w": 24, "x": 0, "y": 8 },
      "id": 3,
      "title": "HTTP 5xx errors",
      "type": "timeseries",
      "targets": [
        {
          "expr": "sum(rate(era_http_requests_total{code=~\"5..\"}[5m])) by (service)",
          "legendFormat": "{{service}}"
        }
      ]
    }
  ],
  "schemaVersion": 39,
  "tags": ["era-one", "xdr"],
  "time": { "from": "now-6h", "to": "now" },
  "title": "ERA One — Platform",
  "uid": "era-one-platform",
  "version": 1
}
