[package]
name = "era-office-richtext"
version = "0.1.0"
edition = "2021"
description = "Shared block + InlineSpan model and span-preserving ops for ERA Docs / Presentations"
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
uuid = { version = "1", features = ["v4"] }
